Public Class FloorHallways
    Public Sub New(floorNumber)

        ' This call is required by the designer.
        InitializeComponent()

        Label1.Text = floorNumber + "01"
        Label2.Text = floorNumber + "02"
        Label3.Text = floorNumber + "03"
        Label4.Text = floorNumber + "04"
        Label5.Text = floorNumber + "05"
        Label6.Text = floorNumber + "06"
        ' Add any initialization after the InitializeComponent() call.

    End Sub

End Class